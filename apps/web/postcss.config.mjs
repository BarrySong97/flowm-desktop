/**
 * @purpose Wire Tailwind CSS v4 into the Next.js PostCSS pipeline.
 * @role    PostCSS config for the apps/web package.
 * @deps    @tailwindcss/postcss.
 */

const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
}

export default config
